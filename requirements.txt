# MINIMAL (stdlib + pyyaml — works immediately, no further dependencies)
pyyaml>=6.0

# STANDARD (API + WebSocket + LLM HTTP calls)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
aiohttp>=3.9.0
httpx>=0.26.0
psutil>=5.9.0

# FULL (all features)
docker>=7.0.0
sentence-transformers>=2.2.0
vosk>=0.3.45
pyaudio>=0.2.13
