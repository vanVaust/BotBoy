# Leverage Patterns

Use these patterns when shaping dense engineering work:

1. Move shared behavior into the narrowest common control point.
2. Add one adapter instead of several call-site workarounds.
3. Fix the contract, then let dependents inherit the correction.
4. Add one well-placed assertion or regression at the seam.
5. Prefer structure that reduces future edits, not only today's patch size.
