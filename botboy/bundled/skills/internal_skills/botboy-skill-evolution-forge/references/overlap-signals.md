# Overlap Signals

These are signs that a skill family needs evolution:

- the descriptions reuse the same verbs
- the default prompts sound interchangeable
- two skills point at the same scripts or references
- a single skill has grown several unrelated checklists
- a user cannot predict which skill should be invoked

When overlap is real, solve it structurally instead of patching wording forever.
