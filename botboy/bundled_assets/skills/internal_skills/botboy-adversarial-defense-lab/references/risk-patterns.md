# Risk Patterns

Look for these patterns:

- unsafe defaults
- silent fallback to weaker security
- missing auth or approval boundaries
- path traversal and file exposure
- prompt injection and instruction override
- unbounded input growth
- command routing ambiguity

Translate each pattern into a named test or mitigation.
