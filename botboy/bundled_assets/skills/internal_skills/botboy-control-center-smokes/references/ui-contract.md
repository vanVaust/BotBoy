# UI Contract

Treat these bindings as high value:

- trace totals and status data
- monitoring summary
- completed capabilities
- open priorities
- deferred items
- eval replay status
- canonical UI path and prototype path

When the payload changes, inspect:

1. `web/index.html`
2. `botboy/__main__.py` dashboard payload generation
3. `docs/STATUS_SNAPSHOT.json`
4. `tests/test_dashboard_payload.py`
